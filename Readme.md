1. What is the difference between var, let, and const?
   Answer :
   Var : It allows the same variable again for example : a similar variable name can have different values.
   let : It don't allow the same variable keeping the different value but we can change and update the value.
   count : It don't allow the same variable keeping the different value and we can't change and update the value.

2. What is the spread operator (...)?
   Answer :
   It spread all items of array or object and we can create a new array or object .

3. What is the difference between map(), filter(), and forEach()?
   Answer :
   forEach() don't return something , map() return something and filter() works based on condition.

4. What is an arrow function?
   Answer :
   It is a short way to write a function.
5. What are template literals?
   Answer : It is `` and we can write into it html code and strings.
